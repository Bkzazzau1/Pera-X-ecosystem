name: Pera-X Contracts CI

on:
  workflow_dispatch:

jobs:
  maintenance:
    name: Contract correction in progress
    runs-on: ubuntu-latest
    steps:
      - run: echo "Contract CI is temporarily manual-only while correction number 1 is hardened atomically on main."
